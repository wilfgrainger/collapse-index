{
  "name": "uk-collapse-index",
  "version": "0.1.0",
  "description": "A transparent, high-frequency UK national stability and systemic stress monitor.",
  "type": "module",
  "private": false,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test",
    "check": "npm run lint:syntax && npm test",
    "lint:syntax": "node --check src/index.js && node --check src/scoring.js && node --check src/config.js && node --check src/demo.js && node --check src/repository.js && node --check public/app.js",
    "generate:bootstrap": "node scripts/generate-bootstrap.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
